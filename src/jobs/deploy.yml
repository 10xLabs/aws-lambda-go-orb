description: >
  This job run the AWS Lambda Go function tests

executor: node

parameters:
  working-directory:
    type: string
    default: deploy

steps:
  - checkout
  - deploy:
    working-directory: << parameters.working-directory >>
